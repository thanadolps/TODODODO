procs:
  task_service:
    cwd: task_service
    shell: sqlx migrate run && cargo run
  notification_service:
    cwd: notification_service
    shell: cargo run
  viz_service:
    cwd: viz_service
    shell: sqlx migrate run && cargo run
  account_service:
    cwd: account_service
    shell: sqlx migrate run && cargo run
